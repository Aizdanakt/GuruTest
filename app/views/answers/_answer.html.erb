<article class="card mt-3">
  <div class="card-body">
    <section>
      <p class="my-2">
        <%= answer.body %>
      </p>
    </section>

    <%= link_to 'Show', answer_path(answer), class: 'btn btn-primary' %>
    <%= link_to 'Update', edit_answer_path(answer), class: 'btn btn-warning' %>
    <%= link_to "Delete", answer_path(answer),class: 'btn btn-danger',
                data: { turbo_method: :delete, turbo_confirm: 'Are you sure?' } %>
  </div>
</article>
